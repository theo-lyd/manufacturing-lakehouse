# manufacturing-lakehouse
A reproducible local “manufacturing lakehouse” that ingests public datasets, stores raw Parquet, implements transformation layer with dbt, orchestrates runs, enforces data quality, and exposes analytics tables + metrics (OEE, downtime, MTTR, scrap rate).
